# Dataset

`raw/arabic_sentiment.txt` contains one Arabic social-media text and one label per line, separated by a tab.

The project code maps the four original labels to the three categories required in the assignment:

- POS → positive
- NEG → negative
- OBJ + NEUTRAL → objective_neutral
